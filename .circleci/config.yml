workflows:
  version: 2
  build:
    jobs:
      - Test

version: 2.0

jobs:
  test:
    docker:
      - image: python:3.7
    steps:
       - checkout
       - run:
          name: Install dependencies
          command: pip install -r requirements.txt
       - run:
          name: Test
          command: pytest

